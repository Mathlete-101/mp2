<?xml version="1.0"?>
<root main_tree_to_execute="MainTree">
    <BehaviorTree ID="MainTree">
        <Sequence name="DefaultBehavior">
            <!-- System Check -->
            <Action ID="CheckSystemStatus" />
            
            <!-- Safety Monitor -->
            <Action ID="MonitorSafety" />
            
            <!-- Wait for Mission Start -->
            <Condition ID="WaitForMissionStart" />
        </Sequence>
    </BehaviorTree>
</root> 